SOLUTION FOR PROBLEM ALICE AND MARKS, PROBLEM CODE - MARKSTW, CODECHEF

#include <bits/stdc++.h>
using namespace std;

int main()
{
    int x, y;
    cin >> x >> y;
    if (x >= (2 * y))
    {
        cout << "Yes";
    }
    else
    {
        cout << "No";
    }
    return 0;
}
